/**
 * 위젯 레벨 컴포넌트 (Widget)
 * - screens에서만 import. 여러 components를 조합한 복합 UI.
 * - 비즈니스/도메인에 가까운 블록 (Header, ProductCard, UserProfile 등).
 * - 예: src/widgets/Header.tsx, src/widgets/ProductCard.tsx
 */

export { Navbar } from './Navbar'
export { Hero } from './Hero'
